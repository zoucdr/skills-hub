# Authoring Notes

This folder exists so the `weryai-video-skill-creator` skill has a visible reference area for repeated guidance and future examples.

It can hold reusable notes for:

- slug selection
- prompt expansion
- output packaging
- rerun and overwrite behavior

The creator skill should keep the user-facing instructions in `SKILL.md` and treat this reference file as supporting material.
