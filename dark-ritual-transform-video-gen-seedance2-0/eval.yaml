version: "1"
defaults:
  trials: 3
  timeout: 600
  threshold: 0.75
skill: "dark-ritual-transform-video-gen-seedance2.0"
trigger_tests:
  - id: explicit-invoke
    should_trigger: true
    prompt: "Use the dark-ritual-transform-video-gen-seedance2.0 skill to transform a boring commute into a styled vertical clip."
  - id: implicit-domain
    should_trigger: true
    prompt: "Gothic candlelit mystery version of this dance practice, slow and moody."
  - id: negative-control
    should_trigger: false
    prompt: "Refactor my Rust crate and add property-based tests only."
outcome_tests:
  - id: layout
    type: deterministic
    checks:
      - file_exists: "SKILL.md"
